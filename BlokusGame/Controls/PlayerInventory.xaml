<UserControl x:Class="BlokusGame.PlayerInventory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BlokusGame"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid x:Name="Container">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="-" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Click="MinimizeControl" FontSize="10"/>

        <Grid x:Name="Inventory" Margin="0,17,0,10" Grid.RowSpan="2">

        </Grid>
    </Grid>
</UserControl>
